# mStudio Extension A11y-Checker

## Local Development

### Sample Data

When using a JetBrains IDE like
[PhpStorm](https://www.jetbrains.com/de-de/phpstorm/) you can easly load some
sample data by executing the HTTP requests defined in
[`sample-data.http`](./sample-data.http). Just execute the file in the IDE.
